// Example action creator test (replace with your actual action creators)
import * as actions from '../src/redux/features/jira/JiraSlice';

describe('jira actions', () => {
  it('should have defined action creators', () => {
    // Replace with your actual action creators if you have any
    expect(actions).toBeDefined();
  });
});
